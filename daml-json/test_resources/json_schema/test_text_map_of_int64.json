{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Fuji.MapTest:Bar",
  "description": "Record (Bar)",
  "oneOf": [
    {
      "type": "object",
      "description": "Record (Bar)",
      "properties": {
        "bar": {
          "description": "TextMap",
          "type": "object",
          "additionalProperties": {
            "description": "Int64",
            "type": [
              "integer",
              "string"
            ]
          }
        }
      },
      "additionalProperties": false,
      "required": [
        "bar"
      ]
    },
    {
      "type": "array",
      "description": "Record (Bar, fields = [bar])",
      "items": [
        {
          "description": "TextMap",
          "type": "object",
          "additionalProperties": {
            "description": "Int64",
            "type": [
              "integer",
              "string"
            ]
          }
        }
      ],
      "minItems": 1,
      "maxItems": 1
    }
  ]
}